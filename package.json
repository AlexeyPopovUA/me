{
  "name": "me",
  "version": "1.0.0",
  "description": "Personal blog and portfolio website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "pnpm install",
    "dev-front": "pnpm --filter front-end dev",
    "update-dependencies-all": "pnpm -r update-dependencies",
    "update-dependencies": "npx npm-check-updates -u --packageFile package.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": "true",
  "workspaces": [
    "./back-end/*",
    "cloud",
    "front-end"
  ],
  "devDependencies": {
    "@tsconfig/node24": "^24.0.0"
  }
}
