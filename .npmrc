# Use a strict peer dependencies policy
auto-install-peers=true

# Enable hoisting for better compatibility
hoist=true

# Shamefully hoist for compatibility with tools that expect flat node_modules
shamefully-hoist=true

# Store dir (default is fine, but can be customized if needed)
# store-dir=~/.pnpm-store
