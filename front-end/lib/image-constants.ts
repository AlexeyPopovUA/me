/** Max width for images declared in JSON-LD and served via the image CDN. */
export const STRUCTURED_DATA_IMAGE_WIDTH = 1200;

/** Open Graph image dimensions. */
export const OG_IMAGE_WIDTH = 1200;
export const OG_IMAGE_HEIGHT = 630;

/** Largest dimension used when probing intrinsic image size via transformed URLs. */
export const IMAGE_PROBE_MAX_DIMENSION = 2400;

/** Fallback contain width when full-size probing fails. */
export const IMAGE_PROBE_CONTAIN_WIDTH = STRUCTURED_DATA_IMAGE_WIDTH;
