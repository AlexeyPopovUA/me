# Twitter / X post

**Account limits (free):** 280 characters per post. Links count as **23 characters** each (t.co), even if the URL is longer.

**Attach:** hero image `static-hosting-platform-hero.png` (optional; image does not add to the character count).

---

## Recommended — single post (~227 / 280)

```
Static hosting for vibe coders: one platform, many apps.

1 S3 bucket. 1 CloudFront. Many apps on subdomains. PR → preview URL.

App repos only call a reusable GitHub Actions workflow — no copied infra.

https://oleksiipopov.com/blog/static-hosting-for-vibe-coders/
```

---

## Alternative — shorter hook (~166 / 280)

```
Many static demos, one boring AWS stack: S3 + CloudFront + GitHub Actions reusable workflows.

Per-app URLs, branch previews, thin app repos.

https://oleksiipopov.com/blog/static-hosting-for-vibe-coders/
```

---

## Optional thread (if one tweet feels too tight)

**Tweet 1/2** (~233 / 280)

```
I wanted a minimal static hosting lane for vibe coding: draft → deploy → share a link → iterate.

Not a PaaS. No backend. Many small apps on shared infra.

New write-up: one bucket, one CloudFront, subdomain routing, PR preview URLs.
```

**Tweet 2/2** (~160 / 280)

```
App repos stay thin: triggers + permissions + workflow_call parameters.

Platform repo owns AWS + deploy logic.

Article + open source:

https://oleksiipopov.com/blog/static-hosting-for-vibe-coders/
```

---

## Hashtags (pick 1–2 max on free tier — they eat characters)

`#AWS` `#CloudFront` `#GitHubActions` `#StaticHosting`

Example with one tag (~250 / 280):

```
One platform, many static apps: S3 + CloudFront + reusable GitHub Actions workflows. Branch previews on PRs, ~20 lines of YAML per app repo.

https://oleksiipopov.com/blog/static-hosting-for-vibe-coders/

#AWS
```
